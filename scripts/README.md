# Скрипты управления

## make-admin.ts

Скрипт для назначения роли администратора пользователю.

### Использование

**Назначить администратора:**
```bash
npm run make-admin email@example.com
```

или напрямую:
```bash
npx tsx scripts/make-admin.ts email@example.com
```

**Снять роль администратора:**
```bash
npx tsx scripts/make-admin.ts email@example.com --remove
```

### Примеры

```bash
# Назначить админа
npm run make-admin admin@example.com

# Снять админку
npx tsx scripts/make-admin.ts user@example.com -r
```

### Панель администратора

После назначения роли администратора, пользователь получит доступ к панели управления по адресу `/admin`.

**Возможности панели:**
- 👥 Просмотр всех пользователей
- 📝 Просмотр списков покупок каждого пользователя
- 🗑️ Удаление пользователей (с каскадным удалением всех данных)
- 🗑️ Удаление списков покупок

### API endpoints

Для администратора доступны следующие API endpoints:

- `GET /api/admin/users` - получить всех пользователей с их списками
- `DELETE /api/admin/users/[id]` - удалить пользователя
- `DELETE /api/admin/shopping-lists/[id]` - удалить любой список покупок

Все запросы к этим endpoints требуют авторизации и роли администратора.
